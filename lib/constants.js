const KEY = 'key'
const CHILDREN = 'children'
const REF = 'ref'

const SECTION_PATH_PROP = 'data-key'
const SECTION_VALUE_PROP = 'data-value'

const RESERVED = [KEY, CHILDREN, REF, SECTION_PATH_PROP, SECTION_VALUE_PROP]

module.exports = {
  KEY,
  CHILDREN,
  REF,
  SECTION_PATH_PROP,
  SECTION_VALUE_PROP,
  RESERVED,
}
